server:
  port: 9090

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
      - id: COVAN-PRODUCER
        uri: lb://COVAN-PRODUCER
        predicates:
          - Path=/cases/**
#        filters:
#        - name: CircuitBreaker
#          args:
#            name: COVAN-PRODUCER
#            fallbackUri: forward:/caseProducerFallBack
      - id: COVAN-ANALYSE
        uri: lb://COVAN-ANALYSE
        predicates:
          - Path=/analyse/**
#        filters:
#        - name: CircuitBreaker
#          args:
#            name: COVAN-ANALYSE
#            fallbackUri: forward:/caseAnalyseFallBack
#
#hysterix:
#  command:
#    fallbackcmd:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 1000
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

